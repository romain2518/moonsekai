<form method="post" action="{{ path('app_calendar-event_delete', {'id': calendar_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendar_event.id) }}">
    <button class="btn">Delete</button>
</form>
