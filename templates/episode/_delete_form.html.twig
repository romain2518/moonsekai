<form method="post" action="{{ path('app_episode_delete', {'work_id': work.id, 'anime_id': anime.id, 'season_id': season.id, 'id': episode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ episode.id) }}">
    <button class="btn">Delete</button>
</form>
